Simulator code lives in this folder.

The **git repository root** is one level up: `LutzLampe/`.

Start from there:

```bash
cd ..
docker compose up --build
```

See [../README.md](../README.md) for Quick Start, Figure 5(b) reproduction, and assumptions.
